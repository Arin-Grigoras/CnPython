#include <stdio.h>

#define len(arr) size_t n = sizeof(arr)/sizeof(sizeof(arr[0]);return n;

/*
 *This is yet another function I cannot implement, PR if you got any ideas.
 * */

int main(void){
	int arr[] = {1, 2, 3};
	int n = len(arr);
	printf("%d", n);
}
